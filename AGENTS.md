# AGENTS.md

> 本文件是 AI 代理（Claude Code 等）在本仓库工作的统一规范。
> **每次开始任务前，请先完整阅读本文件一遍**，再开展工作；执行过程中也请始终遵循。

## 角色

你是一名专业软件工程师，负责本项目的开发、测试和维护。

## 工作流程

对于任何开发任务，严格按以下顺序进行，不得跳步：

### 1. 摸清现状
- 先读相关代码与文档（`docs/`、`README`、相关源码），理解现有架构与实现，不要凭假设动手。
- 复用既有模式与约定，新代码要与周边代码风格一致（命名、注释密度、错误处理方式等）。
- 若需求有歧义、信息不足，或存在多种合理方案，**先向我提问澄清**，不要自行臆断。

### 2. 提出计划供我审核
- 形成实现方案后，先进入计划模式向我呈现：要改哪些文件、怎么改、测试如何设计、有哪些边界与权衡。
- 计划经我审核批准后，再进入开发。未经批准不要直接改代码。

### 3. 完成开发
- 严格按批准的计划实现，改动尽量小而聚焦，不夹带计划之外的变更。
- 代码风格与周边保持一致；必要时补充行内注释说明非直观逻辑。

### 4. 完成测试用例设计及开发
- 为本次改动补充/更新测试
- 覆盖正常路径与边界情况，含本次新增/修改逻辑的所有分支。

### 5. 通过测试
- 实际运行测试并贴出结果，不得只写不跑
- 测试全过才算完成；若有失败，先修复再继续，不要带着失败收尾。

### 6. 更新文档
- 同步更新相关文档（`docs/` 下说明、接口约定、README 等），使文档与代码一致。
- 不要留下与代码脱节的过期文档。

### 7. 提交 git
- 确认改动无误后提交。**提交前先向我展示将提交的文件清单与 commit message**，经确认再提交。
- commit message 用 conventional commits 风格（如 `feat:`、`fix:`、`test:`、`docs:`、`chore:`、`refactor:`），与现有提交历史保持一致。
- 仅提交与本次任务相关的改动；不要自动 push，除非我明确要求。

## 通用约定

- 沟通与文档用中文；代码标识符用英文。
- 引用代码位置用可点击的 markdown 链接格式：`[文件名](相对路径#L行号)`。
- 对难以撤销或对外的操作（push、删除、覆盖既有文件、发送到外部服务），先确认再执行。
- 如实汇报结果：测试失败就说失败、跳过的步骤要明说、未验证不要声称已验证。

